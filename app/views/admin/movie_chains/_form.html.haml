= f.error_messages
- f.inputs do
  = f.input :name
  = f.input :address
  = f.input :bio, :as => :text
  = image_tag @movie_chain.logo.url(:medium)
  = f.input :logo, :as => :file
- f.buttons do
  = f.commit_button commit_label

