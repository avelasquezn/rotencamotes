%head
  = formtastic_stylesheet_link_tag
%h1 New theatre for #{@theatre.movie_chain.name}
- semantic_form_for([:admin, @theatre]) do |f|
  = render 'form', {:f => f, :commit_label => 'Create'}
= link_to "Back to #{@theatre.movie_chain.name} ", [:admin, @theatre.movie_chain]

