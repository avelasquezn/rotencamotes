
%h1 Editiando institución
- semantic_form_for [:admin, @award_institution], :html => { :multipart => true } do |f|
  = render 'form', {:f => f, :commit_label => 'Actualizar'}
= link_to 'Ver', [:admin, @award_institution]
|
\#{link_to 'Regresar', admin_award_institutions_path}

