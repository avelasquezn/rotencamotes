%h1 Editando categoría de premio
- semantic_form_for([:admin, @award_category]) do |f|
  = render 'form', {:f => f, :commit_label => 'Actualizar'}
= link_to 'Ver', [:admin, @award_category]
  |
  \#{link_to 'Regresar', admin_award_categories_path}

