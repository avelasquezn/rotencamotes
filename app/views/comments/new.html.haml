
%h1 Agregando comentario sobre opinión #{@comment.post.title}
%h2 De #{@comment.user.full_name}
- semantic_form_for(@comment) do |f|
  = render 'form', {:f => f, :commit_label => 'Crear'}
= link_to 'Regresar', comments_path

