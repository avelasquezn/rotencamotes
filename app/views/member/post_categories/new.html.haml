%h1 New post_category
- semantic_form_for(@post_category) do |f|
  = f.error_messages
  - f.inputs do
    = f.input :post
    = f.input :category
  - f.buttons do
    = f.submit 'Create'
= link_to 'Back', post_categories_path

